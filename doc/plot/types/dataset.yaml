desc_code: |
  {{name}}: <path>;
desc: |
  Set the dataset for the current element. The dataset should be a valid CSV
  (RFC4180) or GeoJSON file.
examples: |
  // load a csv file
  {{name}}: "myfile.csv";

  // load a geojson file
  {{name}}: "myfile.geojson";
