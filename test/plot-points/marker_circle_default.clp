(size 1024px 512px)
(dpi 96)

(points
    data-x (csv test/testdata/point_example.csv x)
    data-y (csv test/testdata/point_example.csv y)
    scale-x-padding 10
    scale-y-padding 20
    shape (circle-o))
