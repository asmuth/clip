width: 1200px;
height: 480px;

plot {
  data: csv('examples/data/point_example.csv');
  x: x;
  y: y;
  group: series;

  axis-y-min: -40;
  axis-y-max: 60;

  axis-x-min: -60;
  axis-x-max: 60;

  layer {
    type: points;
    sizes: z;
    labels: z;
  }
}
