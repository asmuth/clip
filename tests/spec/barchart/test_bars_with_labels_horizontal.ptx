width: 1280px;
height: 480px;

plot {
  data: csv(tests/testdata/gdp_per_capita_2010.csv);
  y: country;
  x: gdp;

  scale-x-min: 0;
  scale-x-max: 22;

  layer {
    type: bars;
    direction: horizontal;
    labels: gdp_label;
  }
}
