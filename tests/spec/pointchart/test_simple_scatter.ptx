width: 400px;
height: 400px;

scale-x-min: 0;
scale-x-max: 200;

scale-y-min: 0;
scale-y-max: 210;

points {
  xs: 10, 60, 160, 130;
  ys: 60, 150, 180, 30;
  sizes: 4pt, 20pt, 8pt, 6pt;
}

axis {
  position: bottom;
  layout: subdivide(5);
}

axis {
  position: top;
  layout: subdivide(5);
}
