width: 1200px;
height: 480px;

plot {
  axis-x-format: datetime("%H:%M:%S");

  data: csv('examples/data/areadata2.csv', time);
  x: x;
  group: series;

  grid-x: geom;

  layer {
    type: area;
    y1: y;
    y2: z;
  }
}
