width: 1200px;
height: 480px;

plot {
  axis-x-labels: datetime("%H:%M:%S");

  data: csv('tests/testdata/areadata2.csv', time);
  x: x;
  group: series;

  grid-x: geom;

  layer {
    type: area;
    y: y;
    y-offset: z;
  }
}
